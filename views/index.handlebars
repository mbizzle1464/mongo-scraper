
{{> jumbotron}}

<div class="section">
    <div class="container-fluid">
        <div class="card-columns">
            {{#each docs}} {{> article/article}} {{/each}}
        </div>
    </div>
</div>